import fs from 'node:fs';

const tsxPath = 'src/pages/FiscalExtractorApp.tsx';
const cssPath = 'src/styles/fiscal-extractor.css';

let tsx = fs.readFileSync(tsxPath, 'utf8');
let css = fs.readFileSync(cssPath, 'utf8');

const importNeedle = `import {\n  BarChart3,\n  Building2,\n  Check,\n`;
if (!tsx.includes(importNeedle)) throw new Error('Lucide import block not found');
tsx = tsx.replace(importNeedle, `import {\n  Activity,\n  AlertTriangle,\n  ArrowDownToLine,\n  ArrowLeftRight,\n  ArrowUpFromLine,\n  BarChart3,\n  Building2,\n  Check,\n  CircleDollarSign,\n  FileCheck2,\n`);

const navEnd = `];\n\nconst currency = new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' });`;
if (!tsx.includes(navEnd)) throw new Error('Navigation boundary not found');
tsx = tsx.replace(navEnd, `];\n\nconst pageHeadingIconByTitle: Record<string, any> = {\n  'Visão geral': LayoutDashboard,\n  Empresas: Building2,\n  Documentos: FileText,\n  Relatórios: BarChart3,\n  'Certificados digitais': ShieldCheck,\n  'Histórico retroativo': History,\n  Configurações: Settings,\n};\n\nconst metricIconByLabel: Record<string, any> = {\n  Documentos: FileText,\n  Entradas: ArrowDownToLine,\n  Saídas: ArrowUpFromLine,\n  Movimentação: CircleDollarSign,\n  'Cobertura XML': FileCheck2,\n  'XML integral': FileCheck2,\n  'Valor médio': Activity,\n  Empresas: Building2,\n};\n\nconst panelIconForTitle = (title: string) => {\n  if (/atenção/i.test(title)) return AlertTriangle;\n  if (/xml/i.test(title)) return FileCheck2;\n  if (/entrada|saída/i.test(title)) return ArrowLeftRight;\n  if (/modelo/i.test(title)) return BarChart3;\n  if (/empresa/i.test(title)) return Building2;\n  if (/documento/i.test(title)) return FileText;\n  if (/plano|configuração/i.test(title)) return Settings;\n  if (/rotina|movimento|ritmo/i.test(title)) return Activity;\n  return BarChart3;\n};\n\nconst currency = new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' });`);

const pageHeadingOld = `function PageHeading({ title, description, actions }: { title: string; description: string; actions?: React.ReactNode }) {\n  return <div className="extractor-page-heading"><div><span className="extractor-eyebrow">WS Extrator Fiscal</span><h1>{title}</h1><p>{description}</p></div>{actions && <div className="extractor-heading-actions">{actions}</div>}</div>;\n}`;
if (!tsx.includes(pageHeadingOld)) throw new Error('PageHeading block not found');
tsx = tsx.replace(pageHeadingOld, `function PageHeading({ title, description, actions }: { title: string; description: string; actions?: React.ReactNode }) {\n  const Icon = pageHeadingIconByTitle[title] || Activity;\n  return <div className="extractor-page-heading"><div><span className="extractor-eyebrow"><Icon className="extractor-page-icon" />WS Extrator Fiscal</span><h1>{title}</h1><p>{description}</p></div>{actions && <div className="extractor-heading-actions">{actions}</div>}</div>;\n}`);

const metricOld = `function Metric({ label, value, detail }: { label: string; value: string; detail: string }) { return <article className="extractor-metric"><p>{label}</p><strong>{value}</strong><span>{detail}</span></article>; }`;
if (!tsx.includes(metricOld)) throw new Error('Metric block not found');
tsx = tsx.replace(metricOld, `function Metric({ label, value, detail }: { label: string; value: string; detail: string }) {\n  const Icon = metricIconByLabel[label] || Activity;\n  return <article className="extractor-metric"><div className="extractor-metric-head"><p>{label}</p><Icon className="extractor-metric-icon" /></div><strong>{value}</strong><span>{detail}</span></article>;\n}`);

const panelOld = `function PanelHeader({ title, subtitle, action }: { title: string; subtitle: string; action?: React.ReactNode }) { return <header className="extractor-panel-head"><div><h2>{title}</h2><p>{subtitle}</p></div>{action}</header>; }`;
if (!tsx.includes(panelOld)) throw new Error('PanelHeader block not found');
tsx = tsx.replace(panelOld, `function PanelHeader({ title, subtitle, action }: { title: string; subtitle: string; action?: React.ReactNode }) {\n  const Icon = panelIconForTitle(title);\n  return <header className="extractor-panel-head"><div className="extractor-panel-title"><Icon className="extractor-panel-icon" /><div><h2>{title}</h2><p>{subtitle}</p></div></div>{action}</header>;\n}`);

const cssPatch = `\n\n/* 21st.dev-inspired icon language: line icons remain unboxed and animate only on interaction. */\n.extractor-eyebrow{display:inline-flex;align-items:center;gap:7px}\n.extractor-page-icon{width:14px;height:14px;stroke-width:1.8;color:var(--ex-accent)}\n.extractor-metric-head{display:flex;align-items:center;justify-content:space-between;gap:12px}\n.extractor-metric-head p{margin:0}\n.extractor-metric-icon{width:20px;height:20px;flex:0 0 auto;stroke-width:1.55;color:rgba(215,182,90,.82)}\n.extractor-panel-title{display:flex;align-items:flex-start;gap:9px;min-width:0}\n.extractor-panel-icon{width:16px;height:16px;flex:0 0 auto;margin-top:1px;stroke-width:1.65;color:#8298ad}\n.extractor-panel-title>div{min-width:0}\n.extractor-primary svg,.extractor-secondary svg,.extractor-text-action svg,.extractor-toolbar svg,.extractor-search svg,.extractor-eyebrow svg,.extractor-metric-icon,.extractor-panel-icon{transform-origin:center;will-change:transform}\n@media (prefers-reduced-motion:no-preference){\n  .extractor-page-icon,.extractor-metric-icon,.extractor-panel-icon,.extractor-primary svg,.extractor-secondary svg,.extractor-text-action svg,.extractor-toolbar svg,.extractor-search svg{transition:transform .24s cubic-bezier(.22,1,.36,1),color .2s ease,opacity .2s ease}\n  .extractor-page-heading:hover .extractor-page-icon{transform:translateY(-1px) rotate(-4deg)}\n  .extractor-metric:hover .extractor-metric-icon{transform:translateY(-2px) scale(1.08);color:#e4c66e}\n  .extractor-panel:hover>.extractor-panel-head .extractor-panel-icon{transform:translateY(-1px) rotate(-4deg);color:#a9bbca}\n  .extractor-primary:hover svg,.extractor-secondary:hover svg,.extractor-text-action:hover svg{transform:translateY(-1px) scale(1.06)}\n  .extractor-primary:hover .lucide-refresh-cw{transform:rotate(90deg)}\n  .extractor-primary:hover .lucide-download{transform:translateY(2px)}\n  .extractor-toolbar label:focus-within>svg,.extractor-search:focus-within>svg{transform:scale(1.08);color:#d7b65a}\n  .extractor-sidebar nav button:hover .extractor-nav-icon svg{transform:translateY(-1px) scale(1.05)}\n}\n@media (prefers-reduced-motion:reduce){.extractor-page-icon,.extractor-metric-icon,.extractor-panel-icon,.extractor-primary svg,.extractor-secondary svg,.extractor-text-action svg,.extractor-toolbar svg,.extractor-search svg{transition:none!important;transform:none!important}}\n`;
if (!css.includes('21st.dev-inspired icon language')) css += cssPatch;

fs.writeFileSync(tsxPath, tsx);
fs.writeFileSync(cssPath, css);
console.log('Extractor icon language patched.');

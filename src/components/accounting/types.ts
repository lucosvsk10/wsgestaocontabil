
// Define all the types used within the accounting section components

export interface NewsItem {
  id: number;
  title: string;
  description: string;
  date: string;
}

export interface LinkItem {
  id: number;
  title: string;
  url: string;
  description: string;
}
